/* What will the following code output?

It will concatenate the two arrays. Since there is no comma after the 3 in the
first array, and there is no comma before the 4 in the second array, it will 
concatenate the numbers to 34. It will log:

1, 2, 34, 5

*/

console.log([1, 2, 3] + [4, 5]);
